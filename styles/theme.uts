// 设计系统 Token（uts 侧引用，例如状态机/原生层颜色与数字人状态）
// 颜色与 main.css 的 @theme 块保持单一事实来源

export type StringMap = { [key: string]: string }

export const THEME: StringMap = {
  navy: '#0F2C4C',
  navyDeep: '#0B2138',
  smart: '#5B6CFF',
  reliable: '#1FB6A6',
  warn: '#E08A00',
  bgLight: '#F7F9FC',
  textPrimary: '#1A2433',
  textSecondary: '#5B6472',
  border: '#E3E8F0',
  surface: '#FFFFFF'
}

// 数字人状态（文档 02 / 04：idle/asking/listening/followup/thinking/summary）
export type AvatarState = 'idle' | 'asking' | 'listening' | 'followup' | 'thinking' | 'summary'

export const AVATAR_STATE_LABEL: StringMap = {
  idle: '待机',
  asking: '提问中',
  listening: '倾听中',
  followup: '追问中',
  thinking: '思考中',
  summary: '总结中'
}
